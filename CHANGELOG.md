# Changelog

## Unreleased

- Bootstrap first starter implementation of `service-lasso-harness`.
- Add Go CLI commands for `version`, `validate-contract`, and stubbed `run`.
- Add starter contract docs, schema, scripts, and `service-template` example.
