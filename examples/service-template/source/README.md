# Example Service Template Artifact

This is a tiny packaged sample artifact used by the local `service-lasso-harness` starter flow.

It is intentionally minimal. The current harness stub only needs a real archive path to exist so local packaging and contract resolution can be exercised end to end.
